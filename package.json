{
  "name": "saidebar",
  "version": "1.0.0",
  "description": "Custom sidebar Chrome extension with AI chat",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "clean": "rm -rf dist",
    "package": "npm run build && node package-crx.js"
  },
  "devDependencies": {
    "crx3": "^1.1.3",
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "lucide": "^1.31.0",
    "marked": "^15.0.4",
    "openai": "^7.4.0"
  }
}
